# VISION — PMDA-SQLite

## 目的

PMDA（医薬品医療機器総合機構）が公開する電子添文XMLを、**項番・見出し・表・階層を
保ったまま**問い合わせ可能なSQLiteデータベースに変換する。

## スコープ

- 電子添文XMLの読み取りと構造化フィールドの抽出（医薬品識別情報・製品規格・相互作用）
- PMDA公式XSLTによる本文レンダリングとセクション単位への正規化
- 上記を格納するスキーマの定義と、生成物の整合性検証

## 非スコープ

以下は本プロジェクトで扱わない。必要になった場合は**別プロジェクト**として立てる。

- 参照層 — CLI / REST API / MCPサーバ / GUI / Webフロントエンド
- 生成したデータベースの配布・ホスティング
- 医学的判断の支援。本データベースは情報提供のみを目的とし、医療上の意思決定に用いない

## 設計原則

1. **本文の正典はPMDA公式XSLT**。項番・見出し文言・表組みをXMLタグから再導出せず、
   `vendor/pmda-styles/` のスタイルシートが出力するものを唯一の真とする
2. **1 XML = 1 medicines レコード**。`package_insert_no` を一意キーとし、
   内容の類似による統合を行わない
3. **構造化フィールドはXMLタグから直接抽出**する。本文Markdownからの再パースに頼らない
4. **vendorのXSLTは改変しない**を既定とする。改変する場合は
   `vendor/pmda-styles/SOURCE.md` の更新手順への追随コストを引き受ける
5. **外部に対する契約はスキーマのみ**。別プロジェクトの参照層が依存してよいのは
   `medicines` / `specifications` / `interactions` / `sections` /
   `medicines_legacy`(VIEW) / `sections_fts`(trigram) の定義であり、
   内部のパイプライン構成・使用言語・ファイル分割は契約に含まれない

## 実装言語

ETL（XML → SQLite）はPythonとlxmlで実装する。中核がXSLT 1.0の適用であり、
lxmlが依存するlibxml2/libxsltが事実上の唯一の実用的処理系であるため。
他言語への移植は原則3の観点でも利得がない（判断の根拠: `LESSONS.md` 2026-08-27）。
