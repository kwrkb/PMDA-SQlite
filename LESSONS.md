# LESSONS.md

このプロジェクトで得た判断・妥協・修正の記録。単純な実装作業は書かず、
「何を選んだか / なぜそうしたか / 同じミスを防ぐルール」を残す。

## 2026-07-28: XSL方式への移行（json_to_db.py → xml_to_db.py）

### 判断1: スキーマ設計前にスパイクで実物を確認した

**何を判断したか**: PMDA公式XSLを使う方針を決めた時点で、いきなり本実装の
スキーマを設計せず、まず「捨てるつもりのスパイク」でXSL出力の実物を見てから
スキーマを固めた。

**なぜそうしたか（他の選択肢と比較）**: 最初に立てた計画は「本実装まで一気に
書く」ものだったが、ユーザーから「スキーマ作成がエグいから仮説検証していく
ゾーンにしよう」と指摘を受けて計画を分割した。結果、7つの仮説のうち
**H6（図表画像はzipに無く参照のみ）が実際には外れていた**
（一括DLデータ内に95,226枚の画像が実在した）。もし最初からスキーマを
確定していたら、画像パスの扱いを後から手戻りすることになっていた。

**同じミスを防ぐルール**: 外部フォーマット（公式スタイルシート、他社API等）
に依存する設計変更では、"読んで理解したつもり" ではなく実際にサンプル数件を
変換・パースして実物を見てから設計を固める。特に「〜のはず」という仮説は
明示的にリストアップし、1つずつ実データで確認する。

### 判断2: ベンダー成果物（PMDA公式XSL）自体のバグを前提に後処理を設計した

**何を判断したか**: PMDA公式XSLが `UseInSpecificPopulations` 配下の項番を
`9.199999999999999` のような浮動小数点誤差込みで出力するバグを発見し、
「公式が出している値だから正しいはず」と鵜呑みにせず、四捨五入で補正する
後処理 (`fix_float_section_no`) を実装に組み込んだ。

**なぜそうしたか**: 4サンプル全件で再現したため、個別ファイルの偶然ではなく
XSL自体の実装（`concat($index, '.', $startIndex+position()-1)` 等の浮動小数点
演算）に起因すると判断した。

**同じミスを防ぐルール**: 後処理を書く際、最初「切り捨て」で実装してしまい
`9.199999999999999 → 9.1`（本来9.2）という誤った補正になっていたことに、
自分でテストを書いて気づいた。**数値の丸め処理は必ず境界値（`9.1999...`,
`9.6999...`, `9.8000...1`）でテストし、切り捨てと四捨五入を混同しない。**

### 判断3: 変換速度のボトルネックを推測で終わらせず実測した

**何を判断したか**: スパイク段階で「18,023件全件を直列変換すると約5時間」と
分かった時点で、「並列化すればいい」と決め打ちせず、まず単一ファイル変換の
内訳（parse/XSLT/tostring）を計測し、ボトルネックがXSLT変換自体（O(n²)的な
`//`参照が原因、文書サイズに対して超線形）であることを確認してから
`multiprocessing` 化の設計に進んだ。

**なぜそうしたか**: もしボトルネックがI/OやDB書き込みだった場合、並列化の
設計（ワーカーで何を行い、親プロセスで何を行うか）が変わる。実測せずに
並列化すると、無駄な箇所を並列化して効果が出ない可能性があった。

**同じミスを防ぐルール**: 「遅い」という問題に対して対策を決める前に、
最低限「どの処理が遅いか」を計測してから対策を選ぶ。今回は
`etree.parse`（0.5ms〜1.2ms）と`XSLT変換`（18ms〜10秒超）の内訳を分離して
初めて、並列化すべきはXSLT変換自体だと確定できた。

### 判断4: 重複排除キーを `(generic_name, manufacturer)` から `package_insert_no` に変更した

**何を判断したか**: 旧実装の重複排除キーが、18,023 XML中 9,888件しか
`medicines` レコードを作らず、約8,100件の添付文書本文を実質捨てていることを
発見し、`package_insert_no`（添付文書番号、XMLごとに一意）をキーにする
「1 XML = 1 medicines」方式に変更した。

**なぜそうしたか**: `(generic_name, manufacturer)` は同一成分・同一製造元の
別添付文書（改訂版違い、剤形違いなど）を誤って同一視してしまう。
`package_insert_no` はXML自体が持つ識別子であり、意味的に「1つの添付文書
ドキュメント」を正確に表す。

**同じミスを防ぐルール**: 重複排除キーを選ぶときは「ドメイン上の同一性」を
表す識別子（この場合は添付文書番号）を優先する。名称ベースのキー
（`generic_name`等）は表記揺れや同名別物のリスクがあるため、専用の
識別子フィールドが存在するなら必ずそちらを使う。

### 判断5: 旧コードのバグをそのまま移植してしまった（`manufacturer`）

**何を判断したか**: `json_to_db.py:extract_manufacturer` は実在しないタグ名
`ManufactAndImporter` を探しており（実際は `Manufacturer`）、常に一致せず
フォールバックの全文抽出（`TypeOfIndustry`+`Name`+`Address`の連結）を返して
いた。`xml_to_db.py` への書き換え時に、この関数を「同じ動きになるように」
移植した結果、バグごと引き継いでいたことに、全件ロード後の整合性チェックで
実データの値（`製造販売元\n\n\n沢井製薬株式会社\n\n\n大阪市淀川区宮原5丁目
2-30`）を目視して初めて気づいた。正しいタグ名 `Manufacturer` に修正し、
`Name/Lang` のみ抽出するよう変更した。

**なぜそうしたか（他の選択肢と比較）**: 旧実装をそのまま関数単位で移植する
のは効率的だが、「動いているように見える」ことと「正しい」ことは別。
このケースは常にフォールバック分岐を通っていたため、テストが通っても
バグが顕在化しない典型例だった。

**同じミスを防ぐルール**: 既存コードを移植するときは「同じ動きになること」
ではなく「XSD/実データと突き合わせて正しいこと」を検証基準にする。特に
外部スキーマのタグ名を参照する箇所は、移植前に実際のXMLファイルでタグの
存在を確認する。

### 判断6: 中断・再実行を前提にローダを設計した（`is_new` フラグ）

**何を判断したか**: `insert_medicine()` が既存 `package_insert_no` に一致する
medicineのIDを返した場合でも、`store_result()` は `interactions`/`sections`
を無条件に再挿入していた（`specifications` はUNIQUE制約で保護されていたが
他の2テーブルは無保護）。戻り値を `Tuple[Optional[int], bool]`（id, is_new）
に変更し、新規作成時のみ挿入するよう修正した。あわせて、最初のバックグラウンド
実行がセッション終了で強制終了された経験から、再実行を `nohup ... & disown`
＋ `-u`/`PYTHONUNBUFFERED=1`（非TTY実行時の出力バッファ対策）で行うよう変更した。

**なぜそうしたか**: 18,023件・約27分かかるロード処理は、ネットワーク切断や
セッション再起動等で「必ずいつか中断される」という前提に立つべきで、
実際に1回目の全件ロードがセッション再起動で中断された。中断後の再実行が
安全でなければ、DBを作り直すたびに手動でのクリーンアップが必要になり、
運用上のリスクになる。

**同じミスを防ぐルール**: 長時間バッチ処理を書くときは「途中で必ず落ちる」
ことを前提に冪等性（同じ入力を複数回処理しても結果が変わらないこと）を
確保する。UNIQUE制約で守られていないテーブルへの挿入は、呼び出し元の
if文だけに頼らず、挿入関数自体が「新規かどうか」を返す設計にする。
また、非TTY環境でのバックグラウンド実行は出力バッファリングで進捗が
見えなくなることがあるため、`-u`/`PYTHONUNBUFFERED=1` を徹底する。

## 2026-07-31: HeaderRefは削除せず、preview.js と同じ参照解決をPython側で再現する

- 却下した案: `a.HeaderRef` を tail ごと削除して孤立カンマを消す（スパイク当初の実装。
  `docs/XSL_SPIKE.md` の「重大な発見2」に記載していた方針）
- 決め手: 最小XMLを実XSLTに通して生HTMLを確認したところ、tail は区切り記号とは限らず
  後続の本文そのものだった:
  `…出血のおそれがある<a class="HeaderRef" href="#HDR_Warnings"></a>。用法の図<img …>を…`
  旧実装の出力は `効能本文。出血のおそれがある![図](fig001.gif)を参照すること。` となり
  「。用法の図」が欠落。さらに `vendor/pmda-styles/js/preview.js`（見出し参照のテキスト挿入）が
  `#Header-data` から `［10.2 参照］` を生成しており、削除では医学的に意味のある相互参照も失われる
- 覆す条件: PMDAがXSLT側で参照文言を直接出力するようスタイルシートを改訂した場合
  （`vendor/pmda-styles/SOURCE.md` の更新手順でXSLを更新した際は要再確認）

## 2026-07-31: インラインテキストの平坦化を `itertext()` から自前の `_inline_parts()` に統一した

- 却下した案: `<p>` 分岐だけ `img` を拾うよう個別対応する（レビュー指摘のスコープはそこだけだった）
- 決め手: `_clean_inline_text()` は `<p>` だけでなく `_table_to_markdown()` /
  `_list_to_markdown()` からも呼ばれており、`itertext()` はテキストを持たない `<img>` を
  必ず落とす。`preview-include.xsl` の `InlineGraphic` 分岐は表セル・リスト項目内にも
  `<img>` を出すため、`<p>` だけ直しても同じ欠落が2箇所残る
- 覆す条件: なし（インライン要素の平坦化は今後も1箇所に集約する）

## 2026-07-31: 旧スキーマDBは自動DROPせず、検出してexit 1し `--recreate` を要求する

- 却下した案: 旧スキーマを検出したら確認なしに `DROP TABLE` して作り直す
- 決め手: 検出のみで中断すれば失うものは無いが、自動DROPは誤検出時に約27分かけた
  ロード結果（18,023件）を無言で破棄する。`CREATE TABLE IF NOT EXISTS` は既存定義を
  変更しないため、放置すると `insert_medicine()` が既存行を `is_new=False` と判定し
  `sections` を1行も作らないまま「成功」と表示される点は塞ぐ必要があった
- 覆す条件: DBが完全に再生成可能な使い捨て資産になり、ロード時間が十分短くなった場合

## 関連ドキュメント

- [[xsl-fizzy-puddle-plan]] スパイク検証・本実装のプラン全文
- `docs/XSL_SPIKE.md` 仮説検証の詳細結果
